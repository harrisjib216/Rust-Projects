/*
if main.rs exists in the src directory
- a binary create with the same name as our package will be automatically created
- main.rs will be the crate root
- the crate root -> root of the crate -> main.rs -> is the source file
the rust compiler starts at when building our crate
- this will be the root module of our crate


*/
fn main() {
    println!("Hello, world!");
}
